#include "main.h"

/**
 * _isupper - function to check for uppercase letters
 * @c: the letter to check
 *
 * Return: return 1 or 0.
 */

int _isupper(int c)

{
	int i = c;

	if (c >= 'A' && c <= 'Z')
		return (i);
	else
		return (0);
}









