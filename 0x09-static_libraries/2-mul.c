#include "main.h"

/**
 * mul - Multiplication of two integers
 * @a: Value to return
 * @b: Value to return
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
